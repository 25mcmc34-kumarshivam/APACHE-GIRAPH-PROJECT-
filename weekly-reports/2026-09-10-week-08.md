# Week 8 Report — 4–10 September 2026

**Scope:** First complete graph experiments and documentation  
**Status:** Verified

## Work completed

- Created four-node and five-node Giraph JSON datasets.
- Uploaded datasets to HDFS and verified them.
- Diagnosed LocalJobRunner, YARN resource waiting, stopped/stale NodeManager, permissions, and Guava API conflicts.
- Built the Guava-relocated Giraph shaded JAR.
- Completed PageRank, out-degree, in-degree, and shortest-path experiments.
- Created reusable scripts and verified result files.
- Created the GitHub repository and detailed documentation/study material.

## Verified algorithm results

- PageRank order: `3 > 2 > 4 > 1 = 5`.
- Out-degree: `1=1, 2=1, 3=2, 4=1, 5=1`.
- In-degree: `1=0, 2=2, 3=3, 4=1, 5=0`.
- Shortest path from 1: `1=0, 2=1, 3=2, 4=3, 5=unreachable`.
- Shortest path from 5: `1=unreachable, 2=2, 3=1, 4=2, 5=0`.

## Main learning

- Giraph uses vertices, messages, synchronized supersteps, and voting to halt.
- A job in `ACCEPTED` is waiting for YARN resources; it is not executing the algorithm.
- `Double.MAX_VALUE` represents an unreachable shortest-path value in this example.
- Dependency compatibility can fail at runtime even after compilation succeeds.

## Material for the 10 September meeting

- Demonstrate the graph input and outputs.
- Explain the failures and fixes, not only the successful command.
- Show the Git repository, installation guide, weekly timeline, and study roadmap.

## Supervisor feedback

- In the 10 September meeting, Sir said that our progress was now clearly
  visible and the work was looking good.
- Sir asked us not to stop after running the examples. We should understand
  the Java programs used for each calculation and be able to explain the
  classes, variables, input format, messages, supersteps, and output.
- The graph used in this week was very small. Sir asked us to try the same
  work on a somewhat larger dataset.
- Sir asked whether one graph dataset could be divided into multiple physical
  files and still be processed together as one dataset.
- Sir asked why we were using JSON input and whether an ordinary text file
  could also be read by Giraph.
- Sir asked us to implement BFS and then repeat the completed calculations on
  the larger dataset.

## Next week's work

- Prepare a larger graph whose answers can still be checked manually.
- Divide the graph into multiple input files and place them together in one
  HDFS input directory.
- Test a plain-text adjacency-list input format instead of JSON.
- Run PageRank, in-degree, out-degree, BFS, and shortest path on the new
  dataset.
- Read and comment the Java source used for in-degree and out-degree so that
  every team member can explain what the program is doing.
- Study how Giraph performs BFS through messages and supersteps rather than
  only copying and running its command.
- Record the input, commands, errors, corrections, outputs, and manual
  verification in GitHub.
