# Week 7 Report — 28 August–3 September 2026

**Scope:** Environment audit and repair  
**Status:** Verified

## What we did

- Used `hduser` for Hadoop services and `mca2025` for Giraph development/submission.
- Audited Java, Hadoop, Giraph, SSH, HDFS, YARN, files, and permissions.
- Corrected the Java 8 path, malformed `hadoop-env.sh`, and duplicate `.bashrc` settings after creating backups.
- Restarted and verified Hadoop services.
- Checked HDFS health and prepared a clean learning directory.

## Main evidence

- Hadoop 2.7.7 output.
- Giraph commit `14a74297` and built artifacts.
- NameNode, DataNode, SecondaryNameNode, ResourceManager, and NodeManager process checks.
- Java 8 process paths and HDFS health report.

## What we learned

- `JAVA_HOME` does not by itself control the `java` selected from `PATH`.
- Service checks must include both processes and cluster registration.

## Next week's work

- Create a tiny graph, run PageRank, and understand each command and output.
