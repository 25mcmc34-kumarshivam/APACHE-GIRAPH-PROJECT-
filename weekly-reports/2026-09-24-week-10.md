# Week 10 Report — 18–24 September 2026

**Project:** Distributed graph algorithms for social network problems using Apache Giraph  
**Status as of 23 September:** Graph Burning simulator working on a verified small graph

## Faculty direction

The supervisor asked the team to study and implement the Graph Burning problem
on the existing Giraph setup. The related diffusion models discussed were
Independent Cascade (ICM), Linear Threshold (LTM), and Susceptible–Infected–
Recovered (SIR). We have documented the confirmed model names and their
different spreading rules in the [study note](../study-material/graph-burning-and-diffusion-models.md).

## Work completed

- Started HDFS and YARN as `hduser` after the lab machine was restarted.
- Verified NameNode, DataNode, SecondaryNameNode, ResourceManager and
  NodeManager with `jps` and one `RUNNING` YARN node.
- Studied Graph Burning as a process with one newly selected source each
  round and fire propagating one edge per round.
- Created a nine-vertex undirected path divided into three input files.
- Calculated the expected burn rounds manually for sources `3:8:6`.
- Added `LearningGraphBurningComputation.java`, a Giraph simulator that stores
  the first burn round at every vertex.
- Added a runner script and a Hinglish code explanation.
- Uploaded the three files into one HDFS input directory.
- Built the Giraph modules and confirmed the new `.class` exists inside the
  shaded examples JAR.
- Ran the simulator successfully and checked all nine output values against
  the manual table. The graph was fully burned by round 3.
- Added a separate [Maven build explanation](../study-material/maven-build-workflow.md)
  so the team can decide independently when to rebuild.
- Tested a deliberately incomplete two-round sequence `2:8`. Four vertices
  burned and five remained unburned; documented the result in a
  [beginner walkthrough](../study-material/graph-burning-two-round-example.md).
- Created a seven-vertex star and ran sources `1:2`. The centre burned in
  round 1 and all six leaves burned in round 2, matching our manual
  prediction; saved the [actual output](../results/graph-burning-star-7/).
- Ran a disconnected six-vertex graph with two paths and sources `2:4`.
  Vertices 5 and 6 stayed unburned after two rounds, as predicted;
  recorded the [result](../results/graph-burning-disconnected-6/).
- Reran the same disconnected graph for three rounds with sources `2:5:4`.
  All six vertices burned, and their first-burn rounds matched the manual
  calculation. This demonstrates coverage by a supplied sequence, not
  automatic source selection or optimality.

## Error and fix

The first run passed `3,8,6` through Giraph's `-ca` argument. Giraph treated
commas as separators between configuration settings, set the sequence to `3`,
and rejected `8`. That run failed before YARN job submission. We changed the
configuration value and Java parser to use `3:8:6`, copied both corrected
files to the server, rebuilt, and reran successfully.

After the successful run, `ps` showed no NodeManager process. Starting it again
registered a new node, while ResourceManager temporarily still displayed the
old registration with three containers. We checked that only one NodeManager
process existed and that no application was `ACCEPTED` or `RUNNING`, then
stopped YARN for a clean restart. This is an environment recovery step, not a
required part of the Graph Burning algorithm. The repeatable checks are in
the [daily lab startup guide](../docs/DAILY-LAB-STARTUP.md).

## Result

| Vertex | First burn round |
|---:|---:|
| 1 | 3 |
| 2 | 2 |
| 3 | 1 |
| 4 | 2 |
| 5 | 3 |
| 6 | 3 |
| 7 | 3 |
| 8 | 2 |
| 9 | 3 |

Actual output and command evidence are in
[`results/graph-burning-path-9/`](../results/graph-burning-path-9/).
For this path, the known formula gives `b(P9)=3`, so the chosen sequence is
optimal. The Giraph program simulates the supplied sequence; it does not find
an optimal sequence by itself.

## Remaining work

- [x] Validate a sequence that does not burn all vertices within its rounds.
- [x] Validate on a star graph.
- [x] Validate an incomplete sequence on a disconnected graph.
- [x] Validate a complete sequence on the disconnected graph.
- [ ] Validate a cycle graph.
- [ ] Define how to reject missing, duplicate, or already burned source IDs.
- [ ] Implement a source-selection heuristic and compare it to exact answers
  on small graphs.
- [ ] Try Graph Burning on the 30-node graph, after deciding whether to treat
  its directed edges as directed spread or build an undirected version.
- [ ] Record supervisor feedback at the Thursday meeting.

## Team contributions and meeting notes

Individual contributions and final supervisor feedback will be completed
after the team review; they are not inferred from a shared terminal session.
